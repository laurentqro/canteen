%h1 Listing entries

%table
  %tr
    %th Title
    %th Url
    %th Author
    %th Image
    %th Summary
    %th Content
    %th Published on
    %th Feed
    %th
    %th
    %th

  - @entries.each do |entry|
    %tr
      %td= entry.title
      %td= entry.url
      %td= entry.author
      %td= entry.image
      %td= entry.summary
      %td= entry.content
      %td= entry.published_on
      %td= entry.feed_id
      %td= link_to 'Show', entry
      %td= link_to 'Edit', edit_entry_path(entry)
      %td= link_to 'Destroy', entry, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Entry', new_entry_path
